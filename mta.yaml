ID: "JenkinsTest"
version: '1.00'

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "JenkinsTest"
    type: html5
    path: .
    parameters:
       version: 1-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist