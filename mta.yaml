_schema-version: "2.0.0"
ID: "JenkinsTest"
version: 100

parameters:
  hcp-deployer-version: "1.2.0"

modules:
  - name: JenkinsTest
    type: com.sap.hcp.html5
    path: com.jhae.test.JenkinsTest
    parameters:
       version: 100-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist