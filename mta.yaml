_schema-version: "2.0.0"
ID: "JenkinsTest"
version: "0.0.1"

parameters:
  hcp-deployer-version: "1.2.0"

modules:
  - name: jenkinstest
    type: com.sap.hcp.html5
    path: .
    parameters:
       version: 0.0.1-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist